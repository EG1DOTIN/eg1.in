---
id: "59"
title: "Pointer Arithmetic & Pointer Comparisons"
heading: "Pointer Arithmetic & Pointer Comparisons"
category: "c tutorials"
tags: ["c tutorials"]
author: "EG1"
createdAt: "2020-08-24"
release_date: "2020-08-24"
updatedAt: "2026-07-18T09:27:14.024Z"
output_image: "img/blog/id59.webp"
short_description: "Pointer Arithmetic: Increment and decrement of pointers:"
end_description: ""
metaKeyword: "NA"
metaDescription: "NA"
active: "1"
---

Suppose,

int x = 17;

int *p;

Now if x has stored in memory address 003FFC01, then p = &amp;x sets the value 003FFC01 in p;

| MA1 | MA2 | MA3 | MA4 | MA5 | MA6 | MA7 | MA8 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 003FFC01 | 003FFC02 | 003FFC03 | 003FFC04 | 003FFC05 | 003FFC06 | 003FFC07 | 003FFC08 |

Suppose int takes 2 bytes for storing a value, then

p++;

sets the value  003FFC03 in p.

The same is true for decrement.

For example, assuming that p has the value 003FFC03, the expression

p--;

causes p to have the value 003FFC01.

**Pointer Comparisons:**

We can compare two pointers in a relational expression.

For example:

```c
if (p < q) printf("p points to lower memory than q\n");
```

```c
/* Stack example */
#include <stdio.h>
#include <stdlib.h>
#define SIZE 17
#define scanf scanf_s

void push(int i);
int pop(void);
int *tos, *p1, stack[SIZE];

int main(void)
{
     int value;
     tos = stack; /* tos points to the top of the Stack */
     p1 = stack; /* initialize p1 */
     do
     {
           printf("Enter value : ");   
           scanf("%d", &value);
           if (value != 0)
                push(value);
           else
                printf("value on top of Stack is %d\n", pop());
     } while (value != -1);
     getchar();
     return 0;
}

void push(int i)
{
     p1++;
     if (p1 == (tos + SIZE))
     {
           printf("Stack is overflow.\n");   
           exit(1);
     }
     *p1 = i;
}

int pop(void)
{
     if (p1 == tos)
     {
           printf("Stack is underflow. \n");
           exit(1);
     }
     p1--;
     return *(p1 + 1);
}
```
