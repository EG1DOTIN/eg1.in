---
id: "48"
title: "if-else statements"
heading: "if-else statements"
category: "c tutorials"
tags: ["c tutorials"]
author: "EG1"
createdAt: "2020-07-05"
release_date: "2020-07-05"
updatedAt: "2026-07-17T11:56:33.769Z"
output_image: "img/blog/id48.webp"
short_description: "Syntax: if (expression) statement1; else statement2; where statement1 or statement2 may consist of a single statement, a block of statements, or an empty statement(;) . The else part is optional."
end_description: ""
metaKeyword: "NA"
metaDescription: "NA"
active: "1"
---

If **expression** evaluates to true (anything other than 0), the statement1 will be executed, otherwise the statement2  will be executed,(in case the **else** part exists.)

Example:

```c
#include <stdio.h>

#define scanf scanf_s

int main()
{
    int number;
    printf("Enter any number: ");
    scanf("%d", &number);

    if (number)
    {
        printf("the number is not zero");
    }
    else
    {
        printf("the number is zero");
    }

    /*
    //Because there are only one statement,
    //above if-else statements can also be written as      
    if (number)
    printf("the number is not zero");
    else
    printf("the number is zero");
    */

    getchar();
    return 0;
}
```

Nested **if**:

A nested **if** is an **if** that is the target of another **if**.

Example:

```c
#include <stdio.h>

#define scanf scanf_s

int main()
{
      int number;
      printf("Enter any number: ");
      scanf("%d", &number);
      getchar();

      if (number > 0)
      {
            printf("the number is greater than zero\n");
            if (number > 10)
            {
                  printf("the number is also greater than 10\n");
                  if (number > 100)
                  {
                        printf("the number is even greater than 100\n");
                        printf("stop checking now...");
                  }
                  else
                        printf("the number is in (10-100] range\n");
            }
            else
                  printf("the number is between (0-10] range");
      }
      else
            printf("the number is either zero or negetive\n");

      getchar();
      return 0;
}
```

[Note: Here we can similarly understand Nested **else**.]

The **if**-**else**-**if** Ladder:

Syntax:

```c
if (expression1)
            statement1;
      else
            if (expression2)
                  statement2;
            else
                  if (statement3)
                        statement4;
            .
            .
            .
```

The conditions are evaluated from the top to down side.

```c
// Example: if - else if - else

#include <stdio.h>

#define scanf scanf_s

int main()
{
    int number;
    printf("Enter any number: ");
    scanf("%d", &number);
    getchar();
    if (number >= 0 && number < 10)
    {
        printf("the number is in range [0,10)\n");
    }
    else if (number >= 10 && number < 100)
    {
        printf("the number is in range [10,100)\n");
    }
    else if (number >= 100 && number < 1000)
    {
        printf("the number is in range [100,1000) \n");
    }
    else if (number >= 1000 && number < 10000)
    {
        printf("the number is in range [1000,10000) \n");
    }
    else
    {
        if (number < 0)
            printf("the number is negative\n");
        else
            printf("the number is greater than or equal to ten thousands\n");
    }
    getchar();
    return 0;
}
```

```c
// Example: if - else Ladder
#include <stdio.h>
#define scanf scanf_s
int main()
{
    int number;
    printf("Enter any number: ");
    scanf("%d", &number);
    getchar();
    if(number < 0)
    {
        printf("number is negative\n");
    }
    else
    {
        if (number < 10)
        {
            printf("number is less than 10");
        }
        else
        {
            if (number < 100)
            {
                printf("number is less than 100");
            }
            else
            {
                if (number < 1000)
                {
                    printf("number is less than 1000");
                }
                else
                {
                    if (number < 10000)
                    {
                        printf("number is less than 10000");
                    }
                    else
                    {
                        if (number < 100000)
                        {
                            printf("number is less than 100000");
                        }
                        else
                        {
                            if (number < 1000000)
                            {
                                printf("number is less than 1 million");
                            }
                            else
                            {
                                printf("number is greater than 1 million");
                            }
                        }
                    }
                }
            }
        }
    }
    getchar();
    return 0;
}
```
