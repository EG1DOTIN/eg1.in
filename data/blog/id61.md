---
id: "61"
title: "Pointers to pointers"
heading: "Pointers to pointers"
category: "c tutorials"
tags: ["c tutorials"]
author: "EG1"
createdAt: "2020-08-24"
release_date: "2020-08-24"
updatedAt: "2026-07-18T09:36:26.564Z"
output_image: "img/blog/id61.webp"
short_description: "We can make a pointer point to another pointer that points to a target value."
end_description: ""
metaKeyword: "NA"
metaDescription: "NA"
active: "1"
---

For example:

```c
int **x;
```

[Note:  these are same.

```c
int ** x;
```

```c
int** x;
```

```c
int **x;
```

]

This situation is called **multiple indirection**, or **pointers to pointers**.

In the case of a pointer to a pointer, the first pointer contains the address of the second pointer, which points to the object that contains the desired value.

Multiple indirections can be carried on to whatever extent desired, but more than a pointer to a pointer is rarely needed. Excessive indirection is difficult to follow and may generate errors.

To access the target value indirectly pointed to by a pointer to a pointer, we must apply the asterisk operator twice.

For example:

```c
#include <stdio.h>
int main(void)
{
     int x, *p, **q;
     x = 10;  p = &x;  q = &p;
     printf("%d", **q); /* print the value of x */
     return 0;
}
```

Here, p is declared as a pointer to an integer and q as a pointer to a pointer to an integer.
