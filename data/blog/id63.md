---
id: "63"
title: "Pointers to Functions"
heading: "Pointers to Functions"
category: "c tutorials"
tags: ["c tutorials"]
author: "EG1"
createdAt: "2020-08-24"
release_date: "2020-08-24"
updatedAt: "2026-07-18T09:42:33.602Z"
output_image: "img/blog/id63.webp"
short_description: "A function has a physical location in memory that can be assigned to a pointer. This address is the entry point of the function and can be used when the function is called. Once a pointer points to a function, the function can be called with help of that pointer. Function pointers also allow functions to be passed as arguments to other functions."
end_description: ""
metaKeyword: "NA"
metaDescription: "NA"
active: "1"
---

Example:

```c
#include <stdio.h>

void print(char* s, int(*p)(char*))
{
     (*p)(s);
}

int main(void)
{
     char s1[80];
     int(*p)(char *); /* function pointer */
     p = printf;     /* assign address of printf function to p */
     printf("Enter a strings:");
     gets(s1);
     print("\nYou entered:", p); /* pass address of printf via p */
     print(s1, p);   /* pass address of printf via p */
     getchar();
     return 0;
}
```

Output:

Enter a strings: www.eg1.in

You entered: www.eg1.in
