---
id: "62"
title: "Initializing Pointers"
heading: "Initializing Pointers"
category: "c tutorials"
tags: ["c tutorials"]
author: "EG1"
createdAt: "2020-08-24"
release_date: "2020-08-24"
updatedAt: "2026-07-18T09:39:56.107Z"
output_image: "cblog/id62.webp"
short_description: "A pointer that does not currently point to a valid memory location is given the value null (which is zero). Null is used because no object will exist at the null address. Thus, any pointer that is null means that it points to nothing and should not be used."
end_description: ""
metaKeyword: "NA"
metaDescription: "NA"
active: "1"
---

One way to give a pointer a null value is to assign zero to it.

For example

```c
char *p = 0;
```

or we can use NULL macro which is already defined to 0 in &lt;stdio.h&gt; header file.

The below code compiles successfully

```c
int main()
{
     int *p;
     getchar();
     return 0;
}
```

But when we use the pointer it generates compile time error that “uninitialized local variable `p` used”. See below example:

```c
#include <stdio.h>
int main()
{
     int *p;
     printf("%p", p);
     getchar();
     return 0;
}
```

For this, we can just initialize the pointer to a null value. The above code compile successfully if we initialize the pointer to 0;

```c
#include <stdio.h>
int main()
{
     int *p = 0;
     printf("%p", p);
     getchar();
     return 0;
}
```

It shows the following output

00000000
